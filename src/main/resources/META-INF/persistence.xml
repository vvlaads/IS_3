<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
             version="2.1">

    <persistence-unit name="PersistenceUnit" transaction-type="RESOURCE_LOCAL">
        <!-- EclipseLink -->
        <provider>org.eclipse.persistence.jpa.PersistenceProvider</provider>

        <non-jta-data-source>java:jboss/datasources/labs</non-jta-data-source>

        <!-- Сущности -->
        <class>lab.data.Coordinates</class>
        <class>lab.data.Location</class>
        <class>lab.data.Movie</class>
        <class>lab.data.Person</class>
        <class>lab.data.util.Operation</class>

        <!-- Настройки БД -->
        <properties>
            <!-- L2 cache -->
            <property name="eclipselink.cache.shared.default" value="true"/>
            <property name="eclipselink.cache.type.default" value="Full"/>
            <property name="eclipselink.cache.provider"
                      value="org.eclipse.persistence.cache.EhcacheCacheProvider"/>
            <property name="eclipselink.cache.provider_config"
                      value="ehcache.xml"/>

            <property name="eclipselink.logging.level.sql" value="FINE"/>

            <!-- JNDI Connector -->
            <property name="eclipselink.session.customizer"
                      value="lab.database.EclipseCustomizer"/>
            <property name="eclipselink.weaving" value="false"/>
        </properties>
    </persistence-unit>
</persistence>

